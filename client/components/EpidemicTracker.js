import React from 'react'

const EpidemicTracker = props => {
  const colors = ['', '', '']

  for (let i = 0; i < props.epidemicCounter; i++) {
    return (
      <div style={styles}>
        <p> {/*props.epidemicCount*/} </p>
      </div>
    )
  }
}

export default EpidemicTracker
