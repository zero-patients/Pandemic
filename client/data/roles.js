const roles = {
  dispatcher: `- Move another player's pawn as if it were your own
  - As an action, move any pawn to a city with another pawn`,
  medic: `- Remove all cubes of one color when doing Treat Disease`
}

module.exports = roles
